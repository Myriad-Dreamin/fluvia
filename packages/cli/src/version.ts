/**
 * The version stamped into a trace header's `runtime.fluvia` field, so a
 * recording says which release produced it.
 *
 * @module @fluvia/cli/version
 */

export const VERSION = '0.0.1-alpha.1'
